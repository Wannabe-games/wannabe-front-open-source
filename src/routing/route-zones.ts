import { ZONES } from './routes.types';

export const redirectRouteZones = [
    ZONES.AUTH,
    ZONES.CONTENT,
    ZONES.WALLET,
    ZONES.VERIFIED,
    ZONES.NOT_FOUND,
    ZONES.INTERNAL_SERVER_ERROR,
];
