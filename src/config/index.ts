const config = {
    creature: {
        truncatedIdLength: 18,
    },
    referralCode: {
        truncatedLength: 15,
    },
    wallet: {
        truncatedIdLength: 10,
    },
};

export default config;
